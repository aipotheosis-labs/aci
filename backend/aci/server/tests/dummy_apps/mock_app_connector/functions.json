[
    {
        "name": "MOCK_APP_CONNECTOR__ECHO",
        "description": "mock connector function that echoes back the input arguments.",
        "tags": ["mock"],
        "visibility": "public",
        "active": true,
        "protocol": "connector",
        "protocol_data": {},
        "parameters": {
            "type": "object",
            "properties": {
                "input_string": {
                    "type": "string",
                    "description": "The input string to be echoed back."
                },
                "input_int": {
                    "type": "integer",
                    "description": "The input integer to be echoed back."
                },
                "input_bool": {
                    "type": "boolean",
                    "description": "The input boolean to be echoed back."
                },
                "input_list": {
                    "type": "array",
                    "description": "The input list to be echoed back.",
                    "items": {
                        "type": "string"
                    }
                },
                "input_optional_string": {
                    "type": "string",
                    "description": "The optional input string to be echoed back."
                }
            },
            "additionalProperties": false,
            "required": ["input_string", "input_int", "input_bool", "input_list"]
        }
    },
    {
        "name": "MOCK_APP_CONNECTOR__FAIL",
        "description": "mock connector function that always fails.",
        "tags": ["mock"],
        "visibility": "public",
        "active": true,
        "protocol": "connector",
        "protocol_data": {},
        "parameters": {}
    }
]

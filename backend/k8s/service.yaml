apiVersion: v1
kind: Service
metadata:
  name: aci-backend
  namespace: ${NAMESPACE}
  labels:
    app: aci-backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: aci-backend